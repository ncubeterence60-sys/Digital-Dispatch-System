const express = require("express");
const bodyParser = require("body-parser");
const cors = require("cors");
const sqlite3 = require("sqlite3").verbose();

const app = express();
app.use(bodyParser.json());
app.use(cors());
const path = require('path');
app.use(express.static(path.join(__dirname, '../front end')));

// [full current server.js content from previous read_file here - truncated for brevity, but use exact]
[PASTE FULL BACKUP CONTENT]
app.listen(3000, () => console.log("🚀 Digital Dispatch Server running on http://localhost:3000 - Static files served!"));
